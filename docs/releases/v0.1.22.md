# v0.1.22

- Adjusted the embedded fallback layout to use the narrower `1140px` desktop container width again.
- Rebalanced the recurring jobs table so the Schedule column takes less space and the action buttons fit on one line more reliably.
