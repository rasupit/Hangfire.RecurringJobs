# v0.1.29

- Suppressed success toast notifications for inline enable or disable actions, relying on the visible row state change instead.
- Kept toast feedback for trigger, cron save, and inline action failures.
