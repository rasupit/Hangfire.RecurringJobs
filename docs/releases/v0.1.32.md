# Hangfire.RecurringJobs v0.1.32

- Added a direct NuGet package link to the README for easier package discovery from the public GitHub repository.
- Added a GitHub Actions publish workflow that restores, builds, tests, packs, and publishes releases to `nuget.org`.
- Prepared the repository for release-driven package publishing through GitHub releases with a `NUGET_API_KEY` repository secret.
