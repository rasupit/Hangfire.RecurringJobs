# Hangfire.RecurringJobs v0.1.31

- Made the recurring jobs admin surface fail safe when Hangfire storage is unavailable without presenting that outage as normal disabled or missing job state.
- Kept the host application stable while showing an explicit unavailable mode in the UI and API.
- Aligned the remaining public naming to `Hangfire.RecurringJobs`.
- Added baseline public-repo readiness files for CI, Dependabot, security reporting, and richer package metadata.
