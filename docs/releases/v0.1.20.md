# v0.1.20

- Improved the embedded `hangfire-extension.css` fallback so the stock library layout behaves more like Bootstrap when no external styles are configured.
- Added responsive container sizing, navbar structure, mobile collapse behavior, and navbar toggler icon styling to the embedded stylesheet.
- Updated host integration tests to match the current layout and stylesheet rendering behavior.
