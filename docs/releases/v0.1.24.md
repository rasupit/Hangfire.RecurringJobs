# v0.1.24

- Refined the recurring jobs layout to align better with host Bootstrap-based site styling.
- Tightened the embedded fallback table sizing so the action buttons line up more reliably on a single row.
